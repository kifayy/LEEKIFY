/** Live browse URL — classic directory grid (production). */
export const BROWSE_PUBLIC_PATH = "/browse";

/** New discovery hub — localhost only; redirects to {@link BROWSE_PUBLIC_PATH} in production. */
export const BROWSE_DEV_PATH = "/browse-schools";
