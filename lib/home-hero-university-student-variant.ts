/**
 * Archived “University student” hero (also referred to as career audience in code).
 *
 * When enabled:
 * - `HeroAudienceToggle` shows (“High Schooler” / “University Student”).
 * - Teal gradient hero, career portrait, rotating “Dream Job / AI Risk / Future Salary” headline.
 * - `HeroCareerStatReels` + `HERO_CAREER_STATS`; Career Match Quiz CTA where wired.
 *
 * Disabled: homepage shows only the high-school/lavender hero; toggle hidden; `isCareerAudience` is always false.
 *
 * Related modules: `components/home/desktop-hero-banner.tsx`,
 * `components/home/hero-figma-design.tsx`, `components/home/hero-career-stat-reels.tsx`,
 * `lib/hero-career-content.ts`, `components/home/hero-belong-cta.tsx`, `components/home/hero-audience-context.tsx`.
 */
export const HOME_HERO_UNIVERSITY_STUDENT_ENABLED = false;
