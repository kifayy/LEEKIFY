import { RefundPolicyRedirect } from "./refund-policy-redirect";

export const metadata = {
  title: "Refund Policy | Leekify",
  description:
    "Leekify refund policy including trial terms, money-back guarantee, and how to request a refund.",
};

export default function RefundPage() {
  return <RefundPolicyRedirect />;
}
